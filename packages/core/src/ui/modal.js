// src/ui/modal.js
import { debugLog } from '../debug.js';
import { trackEvent, trackOrderCompletion } from '../tracking.js';

export function openFurnitureModal(url, sessionId, config) {
    // ⬇️ Paste your full openFurnitureModal(...) body,
    // but remove the function declaration wrapper and use the arguments (url, sessionId, config)
    // e.g. use debugLog as before, keep the iframe postMessage logic, etc.
    //
    // Only small change: instead of reading config/widgetEndpoint here, you'll receive url already built.
}

export function closeFurnitureModal() {
    // ⬇️ Paste your full closeFurnitureModal() body here
}
